name: Github Workflows

on:
  workflow_call:

jobs:
  validations:
    runs-on: ubuntu-latest
    steps:
      run: echo "Implement VALIDATIONS"

  ci:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Implement CI"
      